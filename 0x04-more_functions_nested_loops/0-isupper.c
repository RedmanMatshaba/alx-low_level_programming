#include "main.h"
/**
 * _isuper -program checks for uppercase charecters
 * @c: variable text
 * Return: Always 0.
 */
int _isuper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
