#include "main.h"
/**
 * _islower - programg that checks for lowercase charecter
 * @c: the chareter to check
 * Return: 1 if c is lowercase, and 0 if its not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
